ProjName           : 'LAST'
NodeNumber         : 1
TimeZone           : 2   # hours

# how much hardware connected
LocalTelescopes   :  []

# write the following as string, which will be eval()'d in matlab
RemoteTelescopes  :  '{1,2}' # syntax: {[indices for first slave], [indices for second], ...}

# power switch driver
PowerDriver : [] # [] to force to cell

# camera power output assigments 
#  (sad, but yml configuration doesn't allow a more sophysticated structure)
CameraPowerUnit   : [1, 1]
CameraPowerOutput : [1, 2]

# mount driver (for now, a single one)
MountDriver : inst.XerxesSimulated
MountPowerUnit   : 1
MountPowerOutput : 3

# camera drivers (as many as LocalTelescopes)
CameraDriver : []

# focuser drivers (as many as LocalTelescopes)
FocuserDriver : []
